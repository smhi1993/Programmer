#include<iostream.h>
#include<conio.h>
double IsTriangle(a,b,c);
void main()
    {cout<<"Enter a,b,c="<<endl;
    cin>>a>>b>>c>>endl;
    if (a+b>c && a+c>b && b+c>a)
    {cout<<"a,b,c are make a Triangle"<<endl;}
    else
    {cout<<"a,b,c are not make a Triangle"<<endl;}
    getch()
    return;}
